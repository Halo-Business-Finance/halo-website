// Enterprise Service Worker component - DISABLED
// Service workers were causing infinite reload loops
// Re-enable when caching strategy is properly tested

const EnterpriseServiceWorker = () => {
  // Service worker registration disabled to prevent loading issues
  return null;
};

export default EnterpriseServiceWorker;